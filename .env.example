# Get UW (partner): https://refer.unusualwhales.com/mark-meyer
# Bring your own — never commit real values
UW_API_TOKEN=
WEBHOOK_SECRET=
# Optional notify URL for cloud bot / phone webhook (ears only — no broker keys)
NOTIFY_URL=
# Optional: path for local queue / journals
DATA_DIR=./data
